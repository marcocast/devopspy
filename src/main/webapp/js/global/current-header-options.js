define(['../collections/nav-options', '../defaults/default-header-options' ],
	function(NavOptions, defaultHeaderOptions) {

		'use strict';
		
		return new HeaderOptions( defaultHeaderOptions );
	});
